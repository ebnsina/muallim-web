<!--
	A faint, fixed aurora wash for the whole marketing page — behind every section,
	so the body carries a touch of the hero's color in either theme. Kept subtle so
	it reads as premium light, not decoration; the hero layers a stronger one on top.
-->
<div aria-hidden="true" class="pointer-events-none fixed inset-0 -z-10 overflow-hidden">
	<div
		class="aurora absolute -top-40 left-[6%] size-[45rem] rounded-full blur-3xl"
		style="background: radial-gradient(closest-side, #3a6ff0, transparent 70%); opacity: 0.1;"
	></div>
	<div
		class="aurora-slow absolute top-[36%] right-[2%] size-[42rem] rounded-full blur-3xl"
		style="background: radial-gradient(closest-side, #7c5cf0, transparent 70%); opacity: 0.08;"
	></div>
	<div
		class="aurora absolute bottom-[-12%] left-1/3 size-[40rem] rounded-full blur-3xl"
		style="background: radial-gradient(closest-side, #22a7f0, transparent 72%); opacity: 0.08;"
	></div>
</div>
